# Copy to .env.local and fill in your values.

# URL of the Go backend. Used server-side only — never exposed to the browser.
BACKEND_URL=https://leuzien-production.up.railway.app/api/v1

# App display name shown in UI and browser title.
NEXT_PUBLIC_APP_NAME=Hachi & Lota
